services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: videorecall
      POSTGRES_USER: videorecall
      POSTGRES_PASSWORD: videorecall
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  backend:
    build: ./backend
    command: python manage.py runserver 0.0.0.0:8001
    env_file: .env
    volumes:
      - ./backend:/app
    ports:
      - "8001:8001"
    depends_on:
      - postgres
      - redis

  celery:
    build: ./backend
    command: celery -A config worker -l info
    env_file: .env
    volumes:
      - ./backend:/app
    depends_on:
      - backend
      - redis

  frontend:
    build: ./frontend
    environment:
      VITE_API_BASE_URL: https://ytdlapi.liftl0g.com/api
      VITE_API_BASE_URL_DEV: http://localhost:8001/api

    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "6173:5173"
    depends_on:
      - backend

volumes:
  postgres_data:
